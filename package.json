{
  "name": "inspigo-moengange",
  "version": "1.0.0",
  "description": "all moengange event in here",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "author": "malik@inspigo.id",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts-resolve",
    "dev": "tsup src/index.ts --format cjs --dts-resolve --watch",
    "prepublishOnly": "changesets version",
    "release": "changesets version && changesets publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "@types/js-cookie": "^3.0.3",
    "@types/node": "^20.2.5",
    "tsup": "^6.7.0",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "@moengage/web-sdk": "^1.0.2",
    "js-cookie": "^3.0.5"
  }
}
